/** Comes from `src-tauri/tauri.conf.json` via a Vite define — bump the
 * version there and nowhere else. */
export const APP_VERSION = __APP_VERSION__
export const KAGEROU_REPOSITORY_URL = 'https://github.com/binido/Kagerou'
export const KAGEROU_RELEASES_URL = `${KAGEROU_REPOSITORY_URL}/releases`
